<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hyunjin.fsms.repository.PaMapper">


    <select id="fspPa002S01" parameterType="hashmap" resultType="hashmap">
        <![CDATA[
			{ CALL HJW_TEST.dbo.FSP_PA002_S01 (#{soNo}) }
        ]]>
    </select>

    <select id="fspPa002U01" parameterType="hashmap" resultType="hashmap">
        <![CDATA[
			{ CALL HJW_TEST.dbo.FSP_PA002_U01 (#{soNo},#{status},#{seq},#{itemCd},#{addQty},#{empNo}) }
        ]]>
    </select>

    <select id="fspPa003S01" parameterType="hashmap" resultType="hashmap">
        <![CDATA[
			{ CALL HJW_TEST.dbo.FSP_PA003_S01 (#{startDate},#{endDate},#{bpCd}) }
        ]]>
    </select>

        <select id="fspPa003I01" parameterType="hashmap" resultType="hashmap">
        <![CDATA[
			{
				 CALL HJW_TEST.dbo.FSP_PA003_I01 (#{soNo},#{itemAddNo},#{soQty},#{itemCd},#{poBpCd},#{empNo})
			}
        ]]>
    </select>


</mapper>

